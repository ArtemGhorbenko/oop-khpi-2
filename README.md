Що я виконав в 7 лабораторній робо:
- Дизайн на основі інтерфейсу виконаний за допомогою двох контрактів: `Notifiable` та `Schedulable`.
- Клас `ConsultationSlot` одночасно реалізує два незалежні контракти (множинна реалізація).
- Клієнтський код (клас Main) обробляє масиви об'єктів виключно через типи інтерфейсів, абстрагуючись від конкретних реалізацій.
- Покрито JUnit 5 тестами логіку контрактів та множинної реалізації.
